# URL Shortener API

A backend API for creating and managing shortened URLs using Node.js, Express, and MongoDB.

The project focuses on building a clean API structure with features like URL generation, redirection, authentication, caching, and analytics support.
